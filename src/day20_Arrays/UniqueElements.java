package day20_Arrays;

public class UniqueElements {
    public static void main(String[] args) {
        String[] words = {"Java","java", "Java"," C++"};

        String unique = words[0];

        for (int i = 0; i < words.length; i++) {
          //  if(words[i]){

            }
        System.exit(0);
        }
     }

